import { colors } from "@queoponents/tokens";

export function DecorativeDiv() {
  return (
    <div
      style={{
        width: '100%',
        height: '100%',
        backgroundColor: colors.surface.level1,
        borderRadius: 10
      }} />
  )
}